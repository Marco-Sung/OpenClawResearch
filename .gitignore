venv/
__pycache__/
*.pyc
.pytest_cache/
attack_log.json
.env
# -- generated attack artifacts (rebuilt every transport run) --
tests/transport/out/
tests/transport/dropbox/
 
# -- python cruft --
*.py[cod]
.venv/
*.egg-info/
 
# -- secrets: NEVER commit tokens/PATs --
.env.*
*.pat
*token*
secrets*
credentials*
# ...but DO allow the secret-free template to be shared:
!.env.example
 
# -- mailpit data (only if you add a persistent volume) --
mailpit-data/
.llm_cache.json

# -- Claude Code local settings (per-developer, not shared) --
.claude/settings.local.json

# -- personal reference/planning docs, not part of the graded deliverable --
docs/
